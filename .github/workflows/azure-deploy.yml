name: Azure Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deployment target pending decision
        run: |
          echo "Pendiente de selección de servicios Azure para frontend, backend y base de datos."
